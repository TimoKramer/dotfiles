[opener]
play = [
  { run = 'vlc "$@"', orphan = true, for = "unix" }
]
pdf = [
  { run = 'evince "$@"', orphan = true, for = "unix" }
]
[open]
rules = [
  { mime = "application/pdf", use = "pdf" }
]
