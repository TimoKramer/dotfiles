[Unit]
Description=Mailbox.org rclone bisync (sync + watcher + tray, all-in-one)
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=%h/.local/bin/mailbox-sync.py
Restart=on-failure
RestartSec=5

[Install]
WantedBy=graphical-session.target
